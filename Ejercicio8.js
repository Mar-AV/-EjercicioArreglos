/**Dado un arreglo con palabras, muestra cuántas vocales tiene cada palabra. */

let numeros = [1, 2, 3];
let nuevoNumero = 4;

let nuevosNumeros = numeros.concat(nuevoNumero);

console.log(nuevosNumeros); // [1, 2, 3, 4]

let otrosNumeros = [5,6];
nuevosNumeros = nuevosNumeros.concat(otrosNumeros);

console.log(nuevosNumeros); // [1, 2, 3, 4, 5, 6]